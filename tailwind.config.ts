import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./src/**/*.{ts,tsx}"],
  theme: {
    extend: {
      colors: {
        // โทนทอง/บรอนซ์ ให้เข้ากับโลโก้ Havenz Property — 500/600/700 หยิบสีจากโลโก้มาตรง ๆ
        // (ทองหลัก / บรอนซ์ / น้ำตาลเข้ม) ส่วนเฉดอื่นไล่ต่อให้ครบสเกล
        // หมายเหตุ 500 -> 600 ลดความสว่างเยอะกว่าปกติโดยตั้งใจ เพราะ 600/700 ใช้เป็นพื้นปุ่ม
        // และสีตัวหนังสือบนพื้นขาว ต้องเข้มพอให้ผ่านค่า contrast (ทองสด ๆ อ่านไม่ออกบนขาว)
        brand: {
          50: "#fbf9f0",
          100: "#f4ebd2",
          200: "#e9d9a9",
          300: "#dac177",
          400: "#cfae45",
          500: "#c9a227", // ทองหลักในโลโก้
          600: "#8a6f1f", // บรอนซ์ในโลโก้
          700: "#6b5417", // น้ำตาลเข้มในโลโก้
          800: "#513f11",
          900: "#3a2d0c",
        },
      },
      fontFamily: {
        sans: ["var(--font-sarabun)", "system-ui", "sans-serif"],
      },
    },
  },
  plugins: [],
};

export default config;

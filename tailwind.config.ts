import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./src/**/*.{ts,tsx}"],
  theme: {
    extend: {
      colors: {
        // โทนทอง/บรอนซ์ ให้เข้ากับโลโก้ Havenz Property — 500/600/700 หยิบสีจากโลโก้มาตรง ๆ
        // (ทองหลัก / บรอนซ์ / น้ำตาลเข้ม) ส่วนเฉดอื่นไล่ต่อให้ครบสเกล
        // หมายเหตุ 500 -> 600 ลดความสว่างเยอะกว่าปกติโดยตั้งใจ เพราะทองสด ๆ ใช้เป็นสีตัวหนังสือ
        // บนพื้นขาวไม่ได้ (ได้ contrast แค่ ~2.4:1) 600 จึงเป็นเพดานความสว่างที่ยังอ่านออก
        // ส่วนพื้นปุ่มใช้ทองสด 500 คู่กับตัวหนังสือเข้ม 900 แทนตัวหนังสือขาว จะได้โทนสว่างแต่ยังอ่านชัด
        brand: {
          50: "#fefcf4",
          100: "#fcf3da",
          200: "#f7e6b3",
          300: "#eed483",
          400: "#dfbe4c", // ทองสว่าง — สถานะ hover ของปุ่ม
          500: "#c9a227", // ทองหลักในโลโก้ — พื้นปุ่ม (คู่กับตัวหนังสือ brand-900)
          600: "#8a6f1f", // บรอนซ์ในโลโก้ — สีตัวหนังสือ/ลิงก์บนพื้นขาว
          700: "#6b5417", // น้ำตาลเข้มในโลโก้
          800: "#513f11",
          900: "#3a2d0c", // ตัวหนังสือบนพื้นทอง
        },
      },
      fontFamily: {
        sans: ["var(--font-sarabun)", "system-ui", "sans-serif"],
      },
    },
  },
  plugins: [],
};

export default config;
